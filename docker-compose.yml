version: "3"

services:
  redis:
    image: redis:4.0.6
    command: ["--appendonly", "yes", "--requirepass", "Abcd@1234"]
    volumes:
      - /goc_automation_platform_docker/compose/redis/data:/data
      #- /goc_automation_platform_docker/compose/redis/conf/redis.conf:/etc/redis/redis.conf
    ports:
      - "6379:6379"
    expose:
      - "6379"
    restart: always
  
  db:
    image: mysql:8.0
    container_name: mysql_mike
    environment:
      MYSQL_ROOT_PASSWORD: '123456'
      MYSQL_DATABASE: 'mike'
      MYSQL_USER: 'myuser'
      MYSQL_PASSWORD: 'mypassword'
    ports:
      - "3306:3306"
  
  web:
    build: .
    command: bash -c "python manage.py runserver 0.0.0.0:8222"
    volumes:
      - .:/app
    environment:
      - DEBUG=0
      - DJANGO_ALLOWED_HOSTS=localhost
      - DATABASE_URL=mysql://root:123456@db:3306/goc
    ports:
      - "8222:8222"
    depends_on:
      - mysql
      - redis
    tty: true
    stdin_open: true

  nginx:
    image: nginx:1.14.1
    ports:
      - "80:80"
      - "443:443"
    expose:
      - "80"
      - "443"
    volumes:
      - /net/szhvm00574/fs0/gocautomation_site/goc_automation:/goc_automation # 挂载项目代码
      - /goc_automation_platform_docker/compose/nginx/log:/var/log/nginx # 挂载日志
      - /goc_automation_platform_docker/compose/nginx/nginx.conf:/etc/nginx/nginx.conf # 挂载配置文件
    depends_on:
      - web
    restart: always
